{
    "installCommand": "bun install",
    "buildCommand": "bunx convex deploy --cmd \"bun run build\"",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}